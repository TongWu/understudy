# Understudy — v1 设计决定

替补演员。帮你把稿子写完、排练到位、上台不慌。

设计画布：`design/understudy-v1.html`（重新生成见文末）

---

## v1 范围（已定）

| | 进 v1 | 不进 v1 |
|---|---|---|
| **形态** | 编辑器 + 导出为自包含 HTML | 云端账号、协作 |
| **用词** | 场地时间（不叫「目标」）· 弹药库（不叫「查询抽屉」）· 节 · 旁批 | — |
| **写** | 台本顺序 · 提词/讲稿对开 · 旁批 · 时间预算 · 语速估算 | — |
| **主题** | 纸 / 夜、紧凑 / 舒适 两个令牌开关 | — |
| **排** | 计时排练 · 难度梯度 · **录音（本地）** · 逐节复盘 · 语速回流校准 | 语音转写对稿（默认关，见下） |
| **讲** | 提词卡（一屏 4 条）· 双屏演讲者视图 | 手机遥控 / 手机看词 |
| **AI** | 生成指令 → 外部跑 → 粘回校验 | 应用内直接调 API |
| **分享** | 打印 / 导出 | 只读分享链接（C 的卡片语言，v1 后期或 v2） |

**关于录音与转写。** 录音进 v1：`MediaRecorder`，按节自动切分（节的时间边界本来
就有），**音频不离开这台设备**。转写不进 v1 的默认路径 —— 它要么用浏览器 Web Speech
（中英混说准确率存疑），要么把音频送去第三方 API。后者意味着内容离开设备，而这个工具
的典型用途是内部演讲（内部系统名、未发布的数据、敏感分类）。所以转写做成**可选、默认关，
并在界面上直接写明音频会离开设备**，不埋在设置里。

因此 v1 的「讲后回填」是**边听边填**，不是自动 diff。界面上照实说：
「系统不知道你实际说了什么，只有你知道。」

实现顺序建议：编辑器 → 提词卡 → 计时与复盘 → 纸/夜 → AI 填充 → 双屏 →
（抽间距变量）→ 紧凑/舒适。

---

## 1. 形态

编辑器 + 单文件导出。平时在编辑器里管理多场演讲，随时"导出为自包含 HTML"
带上台用 —— 保留原型"一个文件就能用"的离线可靠性，但平时不必在一个文件里干活。

## 2. 对象模型

```
Production                 一场演讲
  title, occasion, date, audience, language
  target            秒   场地给的时间（硬约束）
  rate              { en: 词/分, zh: 字/分 }   由排练实测校准
  beats[]           台本顺序
  appendix[]        Q&A / 备用说法（不计时）

Beat                       一节 = 一个提词点
  n, title, slideRef
  budget            秒   计划时长
  importance        1–3  时间不够时先砍谁（「只剩 5 分钟」用它）
  firstLine         派生：script 的第一句，双屏角落常驻
  cue[]             提词：上台唯一会看的东西
    { flag?, cols[]?, lead, say? }
    flag ∈ SAY | SLOW | PAUSE | ASK | OPEN
  script            逐字稿（**强调** 标记重读）
  notes[]           旁批：中文、只给自己看
                    —— **台上会显示，但永远不念出口**。它就是给台上的你看的：
                    「说慢」「这里上次被追问」。不显示等于没有这个功能。
                    不打印、不进纯文本导出、不进「给别人」那份自包含副本；
                    「导出」那份是你自己的工作副本，带着它。

Term                       术语表条目
  term              "DV DW Drift"
  say               上台要说的那一句解释
  note?             中文旁批
  firstUsedIn       派生：第一次出现在哪一节 → 提醒你在那里解释一次

QA                         弹药库条目
  q, a
  tags[]            主题，台上按标签秒查
  askedIn[]         哪几场真被问到 —— 「沉淀」的产物，也是排序依据

Run                        一次排练，或一次真讲
  mode              rehearse | live
  difficulty        照读 | 只看提词 | 藏台本顺序 | 随机打断
  perBeat[]         { spent }
  afterNotes[]      每节：漏讲了什么 / 临场加了什么 / 被打断没
```

`notes` 是原型里 `.zh` 的正式化，是整个产品最独特的一层：
**用母语写给自己的表演提示，和要说出口的话物理分离。**
编辑器、提词卡、双屏三处都有，永远是同一个红色。

## 3. 三个数字（贯穿全产品）

| 名字 | 从哪来 | 用途 |
|---|---|---|
| `budget` | 你手填 | 计划 |
| `estimate` | 字数 ÷ 语速 | **写的时候就知道讲不讲得完** |
| `actual` | 排练计时 | 复盘；反过来校准语速 |

估算公式：`estimate = en词数/rate.en*60 + zh字数/rate.zh*60`
默认 en 130 词/分、zh 200 字/分，**每次排练后用实测值覆盖**。

> 这条在实现时被数据纠正过一次，值得记下来。最初的语速是拍脑袋定的；等到手上有了真实的
> 稿子和三次排练计时，反推出来的语速和拍的数字差了两成以上 —— 估算因此系统性偏长，
> 而且是往「你以为讲得完」的方向偏。**语速必须是测出来的，不能是设定的。**
> 内置样例现在遵守同一条规则：它的语速由「稿子词数 ÷ 最近一次排练用时」算出来，
> 所以估算和实际差不到 2 秒。
>
> 逐节仍然对不齐，而且这是信号不是噪声：有的节你放慢重复（实际远长于按字数的估算），
> 有的节你只扫提词跳着讲（实际远短）。**全文稿不是你实际说出口的东西。**
> 复盘的价值正在于把这个差异显示出来，而不是把它抹平。

**重要：挪预算不会让演讲变短。** 只有删稿子才会。复盘的建议必须说清这件事。

## 4. 台上的配速信号

- **本节倒计时** —— 本节 budget 减去已用，剩 15 秒转琥珀，超了转红
- **全场** —— elapsed / Σbudget
- **偏差** —— `elapsed − Σ(已讲完各节的 budget)`，只在换节时重算，
  所以讲的过程中数字不跳。显示成「快 0:35」/「慢 1:02」：
  **永远带字，不靠颜色单独表意。**

## 5. 视觉系统

**方向已定：A（后台 / 台本），暗色写稿作为开关。** 备选方向 B（全程暗色工作台）
与 C（轻卡片）已淘汰 —— B 的好处由「纸 / 夜」开关全部覆盖，C 的低密度不适合
一个核心是"全场时间"的产品。

两个世界，同一套骨架：

- **后台（浅）** 暖纸 `#FAF7F2` / 墨 `#221E19` / 发丝线。写稿、结构、复盘。
- **台上（暖黑）** `#16130F` / 微光 `#F6F0E5`。不是纯白（会打在脸上、毁夜视），
  也不是纯黑（太硬）。全场唯一鲜艳的只有配速信号 —— 那是两米外要读的东西。

顶栏两个开关，**都只换令牌，布局逐像素一致** —— 四个组合是同一份结构的四次
求值，维护成本接近零：

- **纸 / 夜** —— 配色。幻灯片截图在夜间保持亮色：它本来就是一张亮的幻灯片。
- **紧凑 / 舒适** —— 字号、行距、行高、间距；舒适模式下用留白代替发丝线分隔。

实测代价（同样内容）：提词区 +23%、讲稿区 +22%、台本顺序行高 +29% 高度，
约等于一屏少看五分之一。所以是开关，不是选边。

> 落地提示：颜色本来就是变量，纸/夜可以直接做；紧凑/舒适需要先把间距和字号
> 从行内样式抽成一套 scale 变量（一次性小重构）。

字体：`Instrument Serif`（标题、字标）· `IBM Plex Sans / Condensed / Mono`（界面、
微标签、时码）· `Newsreader`（讲稿正文与提词引语 —— 念稿就是读散文，衬线更好读）。

状态色（已过 CVD 校验，见 `_tokens.css`）：

| | 浅 | 深 | 含义 |
|---|---|---|---|
| go | `#17804F` | `#6FBE86` | 稳 / 讲得完 |
| tight | `#C08717` | `#E9A93C` | 偏紧 |
| over | `#B33121` | `#E2634A` | 超了 / 旁批 |

浅色三色通过全部六项检查（含色觉障碍下的相邻区分度）；深色三色恒与文字同现。

## 6. 四个系统（brainstorm 产出）

11 项候选归成 4 个系统 —— 按系统排期，不按功能排期。

### A. 弹药库 —— 台上要「查」的东西只有一个入口

台上按 `Q` 弹出，三个标签页：**可能被问 · 术语 · 备用说法**。可搜、按标签分组。
讲完后把「这场真被问到的」标记回 `QA.askedIn`，下次这些问题排在最前面 ——
**用得越多越准**。术语表在写稿侧还有一份职责：某个词第一次出现时提醒你解释一遍。


### B. 安全网 —— 台上出状况时

常驻（不用按任何键）：
- 最后 60 秒，进度条变成一根缓慢收缩的线，余光可见
- 双屏角落常驻「下一节的第一句话」—— 换节是最容易卡的地方

按键触发：
- **卡壳保险** `↓`：台上唯一的「看讲稿」键 —— 弹出逐字稿并高亮你大概讲到的那一句。
  （曾经有 `S` 从头看讲稿，已并进来：台上打开讲稿几乎总是因为卡了，那一刻你要的就是位置。）
  定位用 `本节已用 ÷ 本节预算 × 稿子长度` 粗估 —— 不需要语音识别就有八成准
- **只剩 N 分钟** ：按 `importance` 和 budget 算出压缩哪几节、整节跳过哪几节，
  并在台本顺序上把跳过的划掉


### 台上按键（通读后统一过一遍）

| 键 | 做什么 | 在哪一屏 |
|---|---|---|
| `← →` | 换节 | 提词卡 |
| `空格` | 起/停计时 | 提词卡 |
| `↓` | 看讲稿（定在估计位置，`↑↓` 微调） | 提词卡 → 卡壳保险 |
| `Q` | 弹药库 | 提词卡 → 弹药库 |
| `T` | 只剩 N 分钟 | 提词卡 → 重排 |
| `B` | 黑屏 | 提词卡 |
| `M` | 标记「这场被问到了」 | 弹药库 |
| `/` | 搜 | 弹药库 |
| `Esc` | 退出当前层 | 所有弹出层 |
| `P` · `⌘⇧F` | 切提词卡 · 交换两块屏 | 双屏 |

`Esc` / `↑↓` 在不同层里含义不同，但都遵循同一条规则（退出当前层 / 在当前列表上移动），
不算冲突。

### 人称：台本里有两层，故意不统一

- **节名**跟着幻灯片标题走（听众视角，如「你要填的橙色区」）—— 这样你能一眼把台本
  对上屏幕。
- **标记**是你自己的速记（讲者视角，如「橙色区：他们填」）—— 提醒你这块归谁。

编辑器右栏的「标记」下面直接写了这句，免得后来的人以为是笔误。

### C. 排练难度 —— 一个梯度，不是两个功能

`照读 → 只看提词 → 藏台本顺序（冷启动）→ 随机打断（干扰演练）`


### D. 写稿反馈 —— 讲稿不是文章

- **口语化提示**：句子过长、出现书面连接词时轻提示
- **术语首次出现**：见 A
- **讲后回填**：讲完一场，逐节记下漏讲的 / 临场加的 / 被打断的，直接改进稿子


---

## 7. 只读分享视图 / 导出（C 的归宿）

方向 C（轻卡片）不做成编辑器的模式 —— 单栏会让你看不到「全场时间」，
而那是这个产品存在的理由。C 的卡片语言留给**别人看到的东西**：

- 只读分享链接：干净的卡片式台本，没有左右栏、没有计时 —— 那些是你的工具，不是给他看的
- 打印 / 导出的纸质台本同理

尚未设计，v1 后期或 v2。v1 先落了这件事的一半：**「给别人」**导出同一个自包含文件，
但去掉旁批（`U.io.strip()`）。语言还是编辑器的语言，卡片语言留给 C。

## 8. AI 填充（v1 折中方案）

四步：放进幻灯片 → 交代场合 → 复制指令 → 粘回结果并校验。

指令由场合信息自动生成，**它本身就是以后接 API 时的 system prompt** ——
所以这一步不是临时脚手架，是提前把资产攒下来。校验器负责补齐缺失的 budget、
拒收 Σbudget > target 的结果。

## 9. 目录

```
viewer/shell.html          外壳
viewer/css/NN-*.css        令牌、基础、各模块样式（按文件名排序拼接）
viewer/js/NN-*.js          编号即加载顺序，见 docs/planning/CONTRACT.md
viewer/build_template.py   拼成 dist/understudy.html
dist/understudy.html       产物：一个文件，file:// 双击即用
tools/make_sample.mjs      生成内置样例（合成内容，见文件头）
tools/shots.mjs            对真实应用截图 → docs/screenshots/
tests/unit/*.test.js       node --test
tests/e2e-*.js             playwright 驱动真实产物
docs/planning/CONTRACT.md  模块间的共享契约
```

```sh
npm run build     # 重新生成 dist/understudy.html
npm test          # 单测 + 端到端
node tools/make_sample.mjs   # 重新生成内置样例
node tools/shots.mjs         # 重新截图
```

> **关于设计稿。** v1 的界面最初画在一组多画板设计稿上，它们的内容整个是作者真实的
> 内部演讲稿，因此没有留在仓库里。设计意图记录在本文档，界面本身已经实现 ——
> `docs/screenshots/` 是真实应用跑在合成样例上的截图，那才是现在的准绳。
