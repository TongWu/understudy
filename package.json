{
  "name": "understudy",
  "private": true,
  "description": "Single-file speech rehearsal and teleprompter tool — dev tooling only; the product itself is one HTML file",
  "license": "MIT",
  "scripts": {
    "build": "python3 viewer/build_template.py",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:e2e": "node tests/e2e-boot.js"
  },
  "devDependencies": {
    "playwright": "^1.54.0",
    "playwright-core": "^1.54.0"
  }
}
